<%= simple_form_for [@guide, @step] do |f| %>
	<%= f.error_notification %>
  <%= f.input :name %>
  <%= f.input :number, hint: "Number should be between 1 and 99", collection: 1..99%>
  <%= f.button :submit %>

<% end %>
